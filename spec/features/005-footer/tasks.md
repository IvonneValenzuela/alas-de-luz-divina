# Tasks · 005 Footer

- [x] Add `--color-footer-text: #fffdf8` and `--color-footer-badge: #2e2620` to the `:root` block in `client/styles/main.css`, grouped near `--color-tagline` with a comment marking them as footer-only one-offs. — present in `main.css` under `/* Footer-only accents */`; the second token was later renamed `--color-footer-icon` (see "Additional work" below) once its role changed from badge background to icon color, same value.
- [x] Document both new tokens in `/AGENTS.md`'s Conventions section (what they're for, footer-only, not to be reused or extended without documenting here first), matching the existing `--color-tagline` bullet's phrasing. — bullet present in `AGENTS.md`, updated to describe `--color-footer-icon`'s current role.
- [x] Build the pill bar in `Footer.tsx`: single fully-rounded container, `background: var(--color-primary)`, flex row with copyright text on the left and the two icon badges grouped on the right. — `<footer id="footer">` with `rounded-full bg-[#d6b26e]` flex row, confirmed in `Footer.tsx`.
- [x] Render the copyright text exactly as `© 2026 Alas de Luz Divina. Todos los derechos reservados.`, `color: var(--color-footer-text)`. — exact string present, `style={{ color: 'var(--color-footer-text)' }}` confirmed.
- [x] Build the two icon badges (Instagram, TikTok) as circular links: `background: var(--color-footer-badge)`, `border: 1.5px solid var(--color-primary)`, icon in `var(--color-surface)`, reusing `SiInstagram`/`SiTiktok` from `react-icons/si` and the exact URLs already in `Hero.tsx` (Instagram `https://instagram.com/alasdeluzdivina`, TikTok `https://www.tiktok.com/@alas.de.luz.divina`), both `target="_blank" rel="noopener noreferrer"` with matching `aria-label`s. — badges built and URLs/`target`/`aria-label` all confirmed, **but the styling this line describes is the pre-restyle design**: real code now uses the cream/dark-icon scheme from the user's later request (`bg-[#faf7f2]`, no border, icon `var(--color-footer-icon)` `#2e2620`), verified live (computed `background-color: rgb(250, 247, 242)`, `border-width: 0px`, `color: rgb(46, 38, 32)`), matching `spec.md`'s current acceptance criteria. Checked off because the real, verified badge work satisfies the authoritative spec — see "Additional work" below for the restyle itself.
- [x] Check layout at mobile and desktop widths per `/AGENTS.md`'s responsive rule: bar stays a single row (no wrapping), no horizontal overflow, padding/text/badge sizing scaled down cleanly at narrow widths. — re-verified headlessly at 375px and 1440px post-restyle: no `document.documentElement` overflow, copyright text renders as 1 line at both widths (badge restyle didn't change any sizing, only color/border).
- [x] Wire `Footer` into `client/App.tsx` directly after `FAQ` (the current last component), per the documented narrative order in `/AGENTS.md` — `Testimonials` remains an unwired empty shell, out of scope here. — `import Footer` + `<Footer />` confirmed as the last entry in `App.tsx`.
- [x] `npm run lint` and `npm run build` pass. — both re-run just now, exit 0, no warnings, build output produced then removed.
- [ ] Playwright tests for this feature to be written separately via `/plan-tests 005-footer`, deferred from feature closure per `/AGENTS.md`'s testing convention. — not yet run; still open, by design.
- [x] Mark all acceptance criteria in `spec.md` as done and move this feature's roadmap line from "Next" to "Done" in `spec/constitution/constitution.md`. — done via `/close-feature`: all 6 acceptance criteria checked, `Status` set to `done`, roadmap entry moved from "Next" to "Done".

## Additional work done beyond the original plan

- [x] Restyled the social icon badges from the originally-planned dark background (`#2e2620`) with a gold border to a cream background (`#faf7f2`) with the icon itself colored `#2e2620` and no border, per a direct user request mid-implementation. `spec.md`'s acceptance criteria already reflect this newer design.
- [x] Renamed the `--color-footer-badge` token to `--color-footer-icon` in `main.css` and `AGENTS.md` to keep the name accurate after its role changed from "badge background" to "icon color" (same `#2e2620` value).
