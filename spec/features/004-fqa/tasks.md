# Tasks · 004 FAQ

- [x] Rename `client/data/fqa.ts` to `client/data/faq.ts` (content, `FaqItem` interface, and `faqItems` export unchanged), matching `spec.md`'s stated path and the `services.ts`/`testimonials.ts` naming convention. — confirmed via `git status` (`R  client/data/fqa.ts -> client/data/faq.ts`), content unchanged.
- [x] Build the accordion in `FAQ.tsx`: single bordered container (`divide-y` between rows, not separate cards) rendering all twelve items from `faqItems`, each row a `<button>` trigger (question text + `+`/`−` indicator) toggling that item's answer, single-open behavior via one `openId` state value (opening a different item closes whichever was previously open). — confirmed in `FAQ.tsx`, and single-open behavior verified live (opening item 2 closed item 1's `aria-expanded`).
- [x] Wire up accessibility: `aria-expanded` on each trigger button reflecting its open state; confirm keyboard operability (Tab to reach a trigger, Enter and Space both toggle it) comes for free from using native `<button>` elements. — `aria-expanded={isOpen}` present on every trigger button in `FAQ.tsx`.
- [x] Render item 7's (`presenciales-o-virtuales`) inline "Unirme a la comunidad 🤍" link after its answer paragraphs, driven generically off `item.link` (not hardcoded to a specific id/order), using the placeholder URL already in the data file. — `item.link &&` block in `FAQ.tsx`.
- [x] Add the section heading (`Preguntas frecuentes`) and intro structure consistent with `Services.tsx`/`Benefits.tsx`'s existing heading pattern. — `<h2>` present, screenshot-confirmed.
- [x] Build the closing CTA row after the accordion: wings icon (`/favicon.png`) on the left, headline "¿Sientes el llamado a conectar contigo?" + supporting line "Este puede ser el momento de escucharte y dar ese primer paso hacia tu proceso." in the center, pill button "Quiero abrirme a mi proceso" on the right linking to `https://wa.me/573019095778`, opening in a new tab, styled with `.button-primary` matching `Services.tsx`'s existing WhatsApp link pattern. — present in `FAQ.tsx`, text/URL matched, `target="_blank"` confirmed, layout screenshot-verified at both widths.
- [x] Style with existing tokens only: `--color-border` for the container/dividers, `--color-text`/`font-heading` for questions, `--color-text-light` for answers and the CTA's supporting line, `--color-primary` for the `+`/`−` indicator. — only existing hex tokens (`#ebe2d7`, `#7a7268`, `#d6b26e`) and `font-heading` used throughout, no new literals introduced.
- [x] Wire `FAQ` into `client/App.tsx` directly after `AboutPaula` (the current last component), per the documented narrative order in `/AGENTS.md` — `Testimonials` and `Footer` remain unwired empty shells, out of scope for this feature. — confirmed in `App.tsx`.
- [x] Diff all twelve rendered questions/answers against `client/data/faq.ts` line by line — no paraphrasing, reordering, or dropped content. — `FAQ.tsx` renders `item.question`/`item.answer`/`item.link.text` by direct binding, no hardcoded/retyped copy, so accuracy is structural; all 12 `order`s 1–12 present, rendered in array order.
- [x] Check layout at mobile and desktop widths (per `/AGENTS.md`'s responsive rule): accordion rows and CTA row (stacked on mobile, left/center/right from `md` up), no horizontal overflow. — verified with headless Chromium screenshots at 375px and 1440px; no overflow (`scrollWidth` ≤ `clientWidth` at both), CTA stacks on mobile and sits left/center/right on desktop.
- [x] `npm run lint` and `npm run build` pass. — both re-run just now, exit 0, no warnings.
- [x] Tests to be created later, deferred separately from feature closure. — `tests/faq.spec.ts` now exists (12 cases: open/collapse toggle, single-open behavior, the question-7 community link, the closing CTA, all-12-collapsed-on-load, content-accuracy against `faq.ts`, keyboard operability, single-container structure, and no-horizontal-overflow at desktop and mobile), with matching locators (`faqSection`, `faqQuestionButtons`, `faqQuestionButton()`, `faqAccordionContainer`, `faqCommunityLink`, `faqCtaHeading`, `faqCtaButton`, `faqCtaIcon`, `openFaqQuestion()`) added to `tests/pages/HomePage.ts`.
- [x] Mark all acceptance criteria in `spec.md` as done and move this feature to "Done" in `constitution/constitution.md`. — done via `/close-feature`: all 8 acceptance criteria checked, `Status` set to `done`, roadmap entry moved from "Next" to "Done".
